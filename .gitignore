# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets - never commit real credentials (SMTP, API keys, etc.)
secrets.env
server/.env

# Machine-specific build/deploy script (Docker Hub user, Portainer webhook URL) -
# copy scripts/deploy.example.sh to scripts/deploy.sh and fill in your own values.
scripts/deploy.sh

# Machine-specific DB sync script (NAS SSH host, container names) -
# copy scripts/sync-db.example.sh to scripts/sync-db.sh and fill in your own values.
scripts/sync-db.sh

# Project-specific standing instructions for Claude Code - not part of the app itself
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
