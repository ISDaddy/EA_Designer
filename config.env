# Application Port Configuration
# These ports can be modified to avoid conflicts with other services running on your machine.

# The port where the React Frontend is served
APP_FRONTEND_PORT=80

# The port where the Node.js API Backend is served
APP_BACKEND_PORT=4001

# The port where the PostgreSQL Database is exposed
APP_DB_PORT=5432
